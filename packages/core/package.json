{
  "name": "@supplement/core",
  "version": "0.0.1",
  "description": "Deterministic safety engine: validation, flagging, critical-value escalation, and interaction screening. The LLM never decides high/low — this does. SPEC §2.2–2.4, §5.2–5.3, §6.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
