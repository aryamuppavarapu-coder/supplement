{
  "name": "functions",
  "version": "0.0.1",
  "private": true,
  "description": "Firebase Cloud Functions (2nd gen). ALL Anthropic calls live here, server-side (SPEC §5, §7.6, §7.7). The deterministic safety engine (@supplement/core) decides high/low — the model only transcribes and explains.",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "gcp-build": "node -e \"process.exit(0)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,storage,auth",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.1",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.6.3"
  }
}
