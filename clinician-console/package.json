{
  "name": "clinician-console",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Tier-2 clinician review + sign-off console (SPEC §3.8, §7.4, §14). Phase 2 — disabled until §14 prerequisites exist.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase": "^11.6.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
