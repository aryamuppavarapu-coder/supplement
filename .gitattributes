# Normalize line endings to LF in the repo so shell scripts in codemagic.yaml (run on macOS
# CI) and all source files are consistent regardless of the OS they were authored on.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.ico binary
